import React from 'react';
import Form from './Form';
import './Header.css';

const Header = () => {
  return (
    <div>
        <h1>Flicker</h1>
        <Form/>
    </div>
  );
};

export default Header;
